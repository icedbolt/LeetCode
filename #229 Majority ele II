def majorityEle(self, nums: List[int]) -> List[int]:
  count = {}
  for n in nums:
    count[n] = count.get(n, 0) + 1
  for n in count:
    if count[n] > len(nums)/3:
      output.append(n)
  return output
---------------------------------------------------------------------
