def isPalindrome(self, x: int) -> bool:
    if x < 0 or (x > 0 and x % 10 == 0):
        return False
        
    digit = x
    rev = 0
    if digit > rev:
        rev = rev*10 + digit % 10
    if digit = rev:
        return True
    else:
        return False
